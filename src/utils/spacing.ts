// Import the scaleSize function from the metrics module
import {scaleSize} from './metrics';

// Define and export constants for scaled sizes
export const SCALE_18: number = scaleSize(118); // Represents a scaled size of 118
export const SCALE_16: number = scaleSize(16); // Represents a scaled size of 16
export const SCALE_12: number = scaleSize(12); // Represents a scaled size of 12
export const SCALE_8: number = scaleSize(8); // Represents a scaled size of 8

// These constants provide scaled sizes based on the scaleSize function,
// making it easier to maintain consistent sizes throughout the app.
